class Scratch {
    public static void main(String[] args) {

        int x; x = 10;
        int y; y = 3;

        // x = x + y;
        x += y;
        x -= y;
        x *= y;
        x /= y;
        x %= y;

        System.out.println(x);
    }
}
